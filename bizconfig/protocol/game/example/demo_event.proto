syntax = "proto3";

package beast.demo;

// 客户端 → 服务端：局内 ping（经 register_instance_route 解析后 submit_event）
message PingRequest {
  string text = 1;
}

// 服务端 → 客户端：ping 推送（EngineContext::send，route: demo.event.pong）
message PingPush {
  string text = 1;
}
