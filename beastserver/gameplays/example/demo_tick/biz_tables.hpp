#pragma once

namespace beast::demo::tick {

// manifest.json / register_biz_table 使用的逻辑表名，须与 biz_export 导出一致。
inline constexpr const char* kNpcTableLogicalName = "example/npc";

} // namespace beast::demo::tick
